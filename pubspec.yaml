name: fiha_khir
description: Flutter app with login/signup and SQLite
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  flutter_bloc: ^8.1.2
  bloc: ^8.1.2
  sqflite: ^2.3.0+1
  sqflite_common_ffi: ^2.2.0
  path: ^1.8.3
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true